algoritmo declaracao_de_variaveis;

variáveis
x : caracter;
y : caracter;
z : matriz[5] de inteiros;
idade: inteiro;
fim-variáveis

início

função soma(x: inteiro, y: inteiro) : inteiro
  res : lógico;
  início
  res := a+b >= 2-a;
  retorne res;
fim

idade := 20;
se (idade >= 18) então
	se (idade < 60) então
		a := 10;
	senão
		b := 20;
	fim-se
senão
	c := 30;
fim-se

fat := x;
res := 1;
enquanto (x <> 0) faça
	res := res*x;
	a := a + b;
	para (x de fat até 1 passo -1) faça
	      faça 
		    res := res*x;
		    a := a + b;
	      enquanto (x > 1); 
	fim-para
fim-enquanto
	
res := 1;
para (x de fat até 1 passo -1) faça
	res := res*x;
	a := a + b;
fim-para

res := 1;	
faça 
	res := res*x;
	a := a + b;
enquanto (x > 1); 

seleciona (x)
	caso (1):
		x := 1;
		a := a + 2;
		parar;
	caso (2):
		b := b + 1;
		c := c + 1;
		parar;
	caso (3):
		d := d + 1;
		d := a < b;
		parar;
	caso (4):
		parar;
	padrão:
		parar;
fim-seleciona
	
	a := "bcde" + "asdasdasdas";
	x := leia();
	y := imprima("aaaaa", b, c, "cccc", d, 'a');

	 // aa
	 /* aaa
	 *
	 a
	 *
	 */
	 
	 a := (a+1) + (b*(c+1+c+2)));
	
fim
