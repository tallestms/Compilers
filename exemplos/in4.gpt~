algoritmo primitivas;
variáveis
 i:inteiro;
 r:real;
fim-variáveis
início
	imprima("oi");
	imprimaln("oioi");
	leia(r);
	//leialn(r);
	i := maximo(10,20);
	r := maximo(10.0,20.0);
	i := minimo(10,20);
  	r := minimo(10.0,20.0);
	i := media(10,20);
	r := media(10.1,20.2);
	imprima(r);
fim
