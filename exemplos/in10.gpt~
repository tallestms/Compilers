algoritmo declaracao_de_variaveis;
função soma(a : inteiro, b:inteiro ):inteiro
início
  retorne a+b;
fim

variáveis
	a : inteiro;
	b : inteiro;
fim-variáveis

início
	a := 10;
	b := 20;	
	a := soma(a,b);
	imprimaln(a);
fim
