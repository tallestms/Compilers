algoritmo declaracao_de_variaveis;

variáveis
	a:inteiro;
	b:lógico;
	c:lógico;
	d:inteiro;
	m:matriz[4][3] de inteiros;
	n:matriz[4] de inteiros;
fim-variáveis

início
	a := 10;
	a := 10 + - a;
	//m := {[1,2,3,4],[4,3,2,1],[0,0,0,0]};
	//n := [1,2,3,4];
	//a := m[0][0] + m[1][1];
	/*
	a := 10;
	d := 20;
	
	/*
	se (a = 10 e verdadeiro) então
	  d := 10;
	  se (a = 10 e verdadeiro) então
	    d := 10;
	    a := a + 20;
	  fim-se
	  a := a + 20;
	senão
	  se (a = 10 e verdadeiro) então
	    d := 1000;
	  senão
	    se (a = 10 e verdadeiro) então
	      d := 1000;
	    fim-se
	    a := 20;
	  fim-se
	fim-se
	*/
	
	//b := verdadeiro e falso;
	//c := a>d e a<d e a>((a+d)*d) ou a<(a+d); //isso ta certo???
	
	/*
	enquanto (d <> 0) faça
		a := a + d;
	  enquanto ( d <> 0 ) faça
		a := a +d;
	  fim-enquanto
	fim-enquanto
	*/

	/*
	faça
	  a := a + d;
	  faça
	    a := a + d;
	      enquanto ( a <> 0 ) faça
		a := a + 10 + 20;
	      fim-enquanto
	  ;enquanto (a <> 0);
	;enquanto (a <> 0);
	*/
	
	/*
	para (a de d até 1 passo -1) faça
		a := a * 10;
		enquanto ( a <> 0 ) faça
		  a := a + 10 + 20;
		fim-enquanto
	fim-para
	*/
	
	/*
	// Exemplo de uso de SWITCH
	seleciona (a)
		caso (1):
			a := 1;
			parar;
		caso (2):
			a := 2;
			parar;
		caso (3):
			a := 3;
		caso (4):
			a := 4;
			parar;
		padrão:
			a := 1;
			parar;
	fim-seleciona
	*/
	//c := falso;
	//b := a > d e d <= a;
	//a := 1 + 2 * 3 * 4 + 5;
	//b := 1 > 2 > 3; deveria dar erro.. resolver depois
	//b := 1 + 1 > 3 + 2 * 1;
fim
