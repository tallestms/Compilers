algoritmo declaracao_de_variaveis;

variáveis
	a:inteiro;
	b:lógico;
fim-variáveis

início
	
	b := verdadeiro e falso;
	//a := 1 + 2 * 3 * 4 + 5;
	//b := 1 > 2 > 3; deveria dar erro.. resolver depois
	//b := 1 + 1 > 3 + 2 * 1;
fim
