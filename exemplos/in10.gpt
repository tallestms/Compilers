algoritmo declaracao_de_variaveis;

função soma(a:inteiro, b:inteiro) : inteiro
início
retorne a;
fim

variáveis

	a : inteiro;
	b : inteiro;
	c : inteiro;
fim-variáveis

início

	a := 10;
	b := 20;
	c := soma(a,b);
	imprimaln(c);
fim
