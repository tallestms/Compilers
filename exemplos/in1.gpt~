algoritmo declaracao_de_variaveis;

função soma(a : inteiro):inteiro
c,d: inteiro;
re : real;
início
re := 10.10;
c := 20;
a := 10 + c; //retornando c como nao declarado e não a
//ada := 20.5;
retorne c;
fim

função soma3(a:inteiro)
c,d: inteiro;
p: real;
início
d := 10;
c := 20;
p := 20.20;
 //chamada de funcao dentro de outra funcao
fim

função soma2(a:real, b:real):real
c,d: inteiro;
p: real;
início
d := 10;
c := 20;
p := 10.10;
//soma3(p);
p := 20.20;
retorne p;
fim

variáveis
	a : inteiro;
	b : inteiro;
	c,d,f : real;
	g : real;
	char : caracter;
	lit : literal;
	z,oi : lógico;
	//mat : matriz[2] de inteiros; 
fim-variáveis

início
	a := 10;
	//b := 20;
	//a := 10 + 10 + b;
	//c := 20.20;
	//b := 20 + b + b;
	//c := soma2(a,b); 
	//soma2(a,b);
	//a := soma2(a,b,c); 
	soma(10);
	soma3(a);
	a := 10;
	c := 10.10 + 20.10;
	b := 10;
	//c := soma(10); //verificar a função dentro da expressao
	//c := 10.10;
	//d := -10.10 + 10;
	//adsad := 20;
	//d := funcao(f,g);
	//f := (10.10e+10 - 10.10)*10.10 - 10;
	//g := -10.10e+10;
	//char := 'c';
	lit := "isso daqui é um texto";
	//z := verdadeiro;
	//z := 10;
	f := 20.20;
	g := 10.10;
	d := 10.10;
	oi := verdadeiro;
	a := b>(a+b+a);
	a := b>soma(a); //erro
	z := b>a e (c<d) e (a>(b+soma(a)) ou (c<(d+c)));
	//mat := {[1,2],[2,1]};
fim
