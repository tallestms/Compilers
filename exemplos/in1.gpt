algoritmo declaracao_de_variaveis;

função soma(a : inteiro)
c,d: inteiro;
re : real;
início
re := 10.10;
c := 20;
a := 10 + c; //retornando c como nao declarado e não a
//ada := 20.5;
//retorne re;
fim

função soma2(a:inteiro, b:inteiro):inteiro
c,d: inteiro;
início
d := 10;
retorne ada;
fim

variáveis
	b : inteiro; //erro, aparecendo como redeclarada o b não está declarado e está na hash
	c,d,f : real;
	g : real;
	char : caracter;
	lit : literal;
	z : lógico;
	//mat : matriz[2] de inteiros; 
fim-variáveis

início
	b := 20; //erro o b não está declarado e está na hash
	a := 10 + 10;
	c := 20.20;
	b := 20 + b + c;
	c := soma2(a,b); //verificar tipo dentro da funcao
	soma(a);
	a := soma2(a,aaaa); //verificar tipo dentro da funcao
	c := soma(a,b) + c; //veirifcar a função dentro da expressao
	//verificar retorno na função, seja com ou sem retorno
	c := 10.10;
	d := -10.10 + 10;
	adsad := 20;
	d := funcao(f,g);
	f := (10.10e+10 - 10.10)*10.10 - 10;
	g := -10.10e+10;
	char := 'c';
	lit := "isso daqui é um texto";
	z := verdadeiro;
	z := 10;
	z := a>b e b<c; //erro
	//mat := {[1,2],[2,1]};
fim
