algoritmo primitivas;
variáveis
 i:inteiro;
 r:real;
 mat2 : matriz[100][100] de literais;
fim-variáveis
início
	imprima("oi");
	imprimaln("oioi");
	leia(r);
	leialn(r);
	i := maximo('a','z');
	r := maximo(10.0,20.0);
	i := minimo(10,20);
  	r := minimo(10.0,20);
	i := media(10,20.2);
	r := media(10.1,20.2);
fim
