algoritmo declaracao_de_variaveis;

variáveis
x : caracter;
y : caracter;
z : matriz[5] de inteiros;
fim-variáveis

início

função soma(x: inteiro, y: inteiro) : inteiro
  res : inteiro;
  início
  res := a + b >= 2 - a;
  retorne res;
fim

fat := a + b >= 2 - a;


fim
