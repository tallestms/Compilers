algoritmo recursao;

função fibonacci(a:inteiro, b:inteiro,c:inteiro)
	d:inteiro;
	i:inteiro;
início
	d:=a+b;
	i:=c-1;
	imprima(d);
	se (c>0) então
	 	imprima(", ");
	 	fibonacci(b, d, i);
	fim-se
fim

variáveis
	a : inteiro;
fim-variáveis

início
	a := 10;
	fibonacci(1,1,((a/2)*2-5)*2);
fim	